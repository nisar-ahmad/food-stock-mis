@model IEnumerable<FMS.Models.FlourMill>

@{
    ViewBag.Title = "Flour Mills";
}

<div class="title">
    @ViewBag.Title
    <div class="float-right">
        @Html.ActionLink("Add New", "Create", null, new { @class = "btn btn-primary" })
    </div>
</div>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Place.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DistrictOffice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Place.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DistrictOffice.Place.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FlourMillId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FlourMillId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FlourMillId })
        </td>
    </tr>
}

</table>
