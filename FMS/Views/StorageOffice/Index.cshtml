@model IEnumerable<FMS.Models.StorageOffice>

@{
    ViewBag.Title = "Storage Offices";
}

<div class="title">
    @ViewBag.Title
    <div class="float-right">
        @Html.ActionLink("Add New", "Create", null, new { @class = "btn btn-primary" })
    </div>
</div>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Place.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Place.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Place.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Place.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StorageOfficeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StorageOfficeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StorageOfficeId })
        </td>
    </tr>
}

</table>
